# OpenAPI generated server

Spring Boot Server


## Generation command

```bash
docker run --rm -v "${PWD}:/local" openapitools/openapi-generator-cli generate \
  -i /local/TMF915_AI_Management_API_v4.0.0.oas.yaml \
  -g spring \
  -o /local/output \
  --group-id org.etsi.osl \
  --artifact-id controllers-tmf915 \
  --api-package org.etsi.osl.controllers.tmf915.api \
  --model-package org.etsi.osl.controllers.tmf915.model \
  --invoker-package org.etsi.osl.controllers.tmf915 \
  --additional-properties=basePackage=org.etsi.osl.controllers.tmf915,useSpringBoot3=true,useTags=true,interfaceOnly=false,delegatePattern=true,java21=true,useJakartaEe=true,documentationProvider=springdoc
```

## Overview

This server was generated by the [OpenAPI Generator](https://openapi-generator.tech) project.
By using the [OpenAPI-Spec](https://openapis.org), you can easily generate a server stub.
This is an example of building a OpenAPI-enabled server in Java using the SpringBoot framework.

The underlying library integrating OpenAPI to Spring Boot is [springdoc](https://springdoc.org).
Springdoc will generate an OpenAPI v3 specification based on the generated Controller and Model classes.
The specification is available to download using the following url:
http://localhost:8080/v3/api-docs/

Start your server as a simple java application

You can view the api documentation in swagger-ui by pointing to
http://localhost:8080/swagger-ui.html

Change default port value in application.properties
